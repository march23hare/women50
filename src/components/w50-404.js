import { LitElement, html } from '@polymer/lit-element';

class W50404 extends LitElement {
  render() {
    return html`
      <p>존재하지 않는 경로 입니다.</p>
    `;
  }
}

customElements.define('w50-404', W50404);
